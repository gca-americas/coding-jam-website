# --- Node / Next.js ----------------------------------------------------------
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# --- Build output ------------------------------------------------------------
build/
dist/

# --- Local env (secrets!) ----------------------------------------------------
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.local.example

# --- GCP service-account keys (defensive — never commit these) ---------------
*-service-account.json
service-account*.json
*-key.json

# --- Local-only data stores --------------------------------------------------
# Admin allowlist for local dev — bootstrapped via `node scripts/seed-admin.mjs`.
# Contains emails; each developer seeds their own, never share via git.
data/admins.json

# --- Logs --------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- OS / Editor -------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# --- Misc --------------------------------------------------------------------
.cache/
coverage/

# --- Claude Code -------------------------------------------------------------
.claude/
